[package]
name = "eco-svm-std"
version = "0.1.0"
edition = "2021"

[features]
mainnet = []

[dependencies]
anchor-lang = { workspace = true }
derive-new = { workspace = true }

[dev-dependencies]
goldie = { workspace = true }
